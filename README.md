# TrajectoryCurer
TrajectoryCurer, an interactive system integrating quality diagnosis with wrangling operations, bridging the gap between trajectory visualization tools (which assume clean data) and general wrangling tools (which lack spatiotemporal semantics).
